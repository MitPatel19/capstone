# Ride Bargain Platform (Server)

Express + SQLite + JWT API for a simple ride-bargaining platform (WhatsApp ride groups, but safer while driving).

## Run

```bash
cd server
npm install
cp .env.example .env
npm run dev
```

API: `http://localhost:5050`

